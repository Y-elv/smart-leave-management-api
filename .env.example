# Server
PORT=8081
NODE_ENV=development

# MongoDB (not exposed publicly in production)
MONGO_URI= 

# JWT
JWT_SECRET= 
JWT_EXPIRES_IN=

# Super Admin (bootstrap; not stored in DB)
SUPER_ADMIN_EMAIL=
SUPER_ADMIN_PASSWORD= 
SUPER_ADMIN_NAME=

# Mail (for invite emails)
MAIL_HOST=
MAIL_PORT=
MAIL_SECURE=false
MAIL_USER=
MAIL_PASS=
MAIL_FROM=

# Optional: base URL for invite links
LOCAL_URL=http://localhost:8081
API_BASE_URL=
